# Build dependencies for the paideia docs site (MkDocs Material).
# Local:  pip install -r requirements.txt && mkdocs serve
# uv:     uvx --with-requirements requirements.txt mkdocs serve
# CI:     used by .github/workflows/docs.yml
mkdocs-material>=9.5
